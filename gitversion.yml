mode: Mainline
commit-message-incrementing: Disabled

branches:
  main:
    regex: ^main$
    increment: Patch
  feature:
    regex: ^(.+\/)?(feature|feat)\/
    tag: alpha
    increment: Minor
  hotfix:
    regex: ^(.+\/)?(hotfix|fix)\/
    tag: alpha
    increment: Patch